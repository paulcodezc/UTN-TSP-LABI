/**
 * \brief Solicita que se ingrese el primer numero
 * \param
 * \return numero_1 retorna el numero ingresado
 *
 */
float pedir_Numero_1();



/**
 * \brief Solicita que se ingrese el segundo numero
 * \param
 * \return numero_2 retorna el numero ingresado
 *
 */
float pedir_Numero_2();



/**
 * \brief Solicita que se realize la suma de los dos numeros ingresados
 * \param numero_1 es el primer numero
 * \param numero_2 es el segundo numero
 * \return resultado retorna el resultado de la suma
 *
 */
float sumar_Numeros(float, float);



/**
 * \brief Solicita que se realize la resta de los dos numeros ingresados
 * \param numero_1 es el primer numero
 * \param numero_2 es el segundo numero
 * \return resultado retorna el resultado de la resta
 *
 */
float restar_Numeros(float, float);



/**
 * \brief Solicita que se realize la division de los dos numeros ingresados
 * \param numero_1 es el primer numero
 * \param numero_2 es el segundo numero
 * \return resultado retorna el resultado de la division
 *
 */
float dividir_Numeros(float, float);



/**
 * \brief Solicita que se realize la multiplicacion de los dos numeros ingresados
 * \param numero_1 es el primer numero
 * \param numero_2 es el segundo numero
 * \return resultado retorna el resultado de la multiplicacion
 *
 */
float multiplicar_Numeros(float, float);



/**
 * \brief Solicita que se calcule el factorial del primer numero (A)
 * \param numero_1 es el primer numero
 * \return factorial retorna el factorial del primer numero (A)
 *
 */
long factorial_Numeros(long);



/**
 * \brief Solicita que se calcule todo (Suma,Resta,Division,Multiplicacion,Factorial)
 * \param numero_1 es el primer numero
 * \param numero_2 es el segundo numero
 * \return
 *
 */
void calcular_Todo(float, float);
